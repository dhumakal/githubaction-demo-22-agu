name: Install PHP

on:
  workflow_dispatch:

jobs:
  install-php:
    runs-on: windows-latest

    steps:
      - name: Install PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'

      - name: Check PHP version
        run: php --version
